/**
 * Surveyed terrain under the sector map. Owned CSS contours keep the registry
 * free of third-party srcDoc frames and remain stable under reduced motion.
 */
export default function SectorTerrain() {
  return <div className="q-terrain" aria-hidden="true" />;
}
